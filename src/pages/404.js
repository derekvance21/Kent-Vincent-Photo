import React from "react"
import Layout from "../components/Layout"

export default () => {
  return (
    <Layout>
      <div className="text-header">
        <h1>
          404 ERROR. We're either still working on that page or it doesn't exist
        </h1>
      </div>
    </Layout>
  )
}
